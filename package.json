{
  "name": "two-ball-breakout",
  "version": "1.0.0",
  "description": "A static browser game with black and white balls flipping grid square colors",
  "scripts": {
    "dev": "npx live-server --port=8080 --no-browser",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint scripts/**/*.js tests/**/*.js",
    "lint:fix": "eslint scripts/**/*.js tests/**/*.js --fix",
    "format": "prettier --write ."
  },
  "keywords": ["game", "p5js", "static", "browser"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "prettier": "^3.0.3"
  }
}

